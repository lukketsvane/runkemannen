export const TILE_SIZE = 32;
export const VIRTUAL_WIDTH = 320;
export const VIRTUAL_HEIGHT = 568; // 9:16 Aspect Ratio
export const FPS = 60;

export const PLAYER_SPEED = 3.0; 
export const NPC_WALK_SPEED = 0.8; 
export const NPC_RUN_SPEED = 2.2; // Reduced from 3.5 to make them less impossible to escape

export const RUNK_DISTANCE = 48; // Distance to perform the action
export const CHARGE_DISTANCE = 160; // Max distance to charge mana
export const CHARGE_RATE = 0.4; // Mana per frame per girl
export const DETECTION_PROXIMITY_THRESHOLD = 50; // Distance at which girls notice moving player

export const SPRITE_SCALE = 1;

// UI Constants
export const AUTO_HIDE_DELAY_MS = 3000; // Time before controls auto-hide (milliseconds)